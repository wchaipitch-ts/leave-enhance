<?xml version="1.0" encoding="UTF-8"?>
<CustomObject xmlns="http://soap.sforce.com/2006/04/metadata">
    <allowInChatterGroups>false</allowInChatterGroups>
    <deploymentStatus>Deployed</deploymentStatus>
    <description>One row per Employee x Leave Type x Year. Replaces the per-type leave fields on User: User is a setup object, so writing balances there forces the deduction into an @future and makes per-type, per-year balances unmodellable. Rows are created and maintained by LeaveManagementController and keyed by External_Key__c.</description>
    <enableActivities>false</enableActivities>
    <enableBulkApi>true</enableBulkApi>
    <enableFeeds>false</enableFeeds>
    <enableHistory>true</enableHistory>
    <enableReports>true</enableReports>
    <enableSearch>true</enableSearch>
    <enableSharing>true</enableSharing>
    <enableStreamingApi>true</enableStreamingApi>
    <label>Leave Balance</label>
    <nameField>
        <displayFormat>LB-{00000}</displayFormat>
        <label>Leave Balance No.</label>
        <type>AutoNumber</type>
    </nameField>
    <pluralLabel>Leave Balances</pluralLabel>
    <sharingModel>Private</sharingModel>
    <visibility>Public</visibility>
</CustomObject>
