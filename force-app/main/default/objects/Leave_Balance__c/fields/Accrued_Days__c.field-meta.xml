<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Accrued_Days__c</fullName>
    <description>Entitlement earned so far. A formula rather than a stored value so it stays correct even if the daily job misses a run. Monthly accrual credits at month end.</description>
    <label>Accrued to Date</label>
    <formula>IF( OR( ISBLANK(Employee__r.Start_Date__c), Leave_Year__c &gt; YEAR(TODAY()) ), 0, IF( Leave_Year__c &lt; YEAR(TODAY()), BLANKVALUE(Entitlement_Days__c, 0), IF( Accrual_Method__c = "Monthly", MAX(0, MIN( BLANKVALUE(Entitlement_Days__c, 0), IF( YEAR(Employee__r.Start_Date__c) = Leave_Year__c, MONTH(TODAY()) - MONTH(Employee__r.Start_Date__c), MONTH(TODAY()) - 1 ) + IF( DAY(TODAY() + 1) = 1, 1, 0) )), BLANKVALUE(Entitlement_Days__c, 0) )))</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <precision>18</precision>
    <scale>1</scale>
    <required>false</required>
    <type>Number</type>
</CustomField>
