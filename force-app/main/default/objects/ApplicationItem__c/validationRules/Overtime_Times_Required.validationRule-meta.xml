<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Overtime_Times_Required</fullName>
    <active>true</active>
    <description>An overtime request with no times claims no hours. Enforced here rather than by making the fields universally required, which would also force them onto every leave request.</description>
    <errorConditionFormula>
        AND(
          RecordType.DeveloperName = "Overtime_Request",
          OR(ISBLANK(OT_Start__c), ISBLANK(OT_End__c))
        )
    </errorConditionFormula>
    <errorDisplayField>OT_Start__c</errorDisplayField>
    <errorMessage>An overtime request needs both an OT Start and an OT End time.</errorMessage>
</ValidationRule>
