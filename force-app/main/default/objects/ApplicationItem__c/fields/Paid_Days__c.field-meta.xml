<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Paid_Days__c</fullName>
    <description>The portion of the request that draws on the leave balance, for payroll. A formula rather than a stored value so it cannot drift from the two fields it is derived from.</description>
    <externalId>false</externalId>
    <formula>BLANKVALUE(Leave_Days__c, 0) - BLANKVALUE(LWOP_Days__c, 0)</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <label>Paid Days</label>
    <precision>18</precision>
    <required>false</required>
    <scale>1</scale>
    <trackTrending>false</trackTrending>
    <type>Number</type>
    <unique>false</unique>
</CustomField>
